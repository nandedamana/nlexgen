// This wrapper file placed inside include/nlexgen helps other programs include
// it using `#include <nlexgen/read.h>`, avoiding confusion with read.h
// of the including program itself (if any) and vice-versa.

#include "../../read.h"
